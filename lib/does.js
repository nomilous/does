// Generated by CoffeeScript 1.6.3
module.exports._does = function() {
  return testable;
};

module.exports.does = function(config) {
  var api, local, mode, testable;
  if (config == null) {
    config = {};
  }
  mode = config.mode || 'spec';
  if (mode === 'spec') {
    local = testable = {
      expect: function() {},
      verify: function() {}
    };
  } else {
    throw new Error("does doesn't " + mode);
  }
  return api = {
    expect: local.expect,
    verify: local.verify
  };
};
